function _extends() { _extends = Object.assign ? Object.assign.bind() : function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }
import React from "react";
import { useState } from "react";
import Slider from "./Slider";
var Template = args => {
  var [selectedValue, setSelectedValue] = useState(50);
  return /*#__PURE__*/React.createElement(Slider, _extends({}, args, {
    onChange: e => setSelectedValue(Number(e.target.value)),
    value: selectedValue
  }));
};
var meta = {
  component: Slider,
  render: Template,
  tags: ["autodocs"],
  argTypes: {
    caution: {
      control: {
        type: "text"
      }
    },
    error: {
      control: {
        type: "text"
      }
    },
    help: {
      control: {
        type: "text"
      }
    },
    label: {
      control: {
        type: "text"
      }
    }
  }
};
export default meta;
export var Default = {
  render: Template.bind({}),
  name: "Default",
  args: {
    disabled: false,
    label: "Volume",
    max: 100,
    min: 0,
    showInput: false
  }
};

/**
 * You can also optionally provide a numeric representation of the slider value.
 */
export var WithInput = {
  render: Template.bind({}),
  name: "With input",
  args: {
    disabled: false,
    inputDisabled: false,
    label: "Volume",
    max: 100,
    min: 0,
    showInput: true
  }
};